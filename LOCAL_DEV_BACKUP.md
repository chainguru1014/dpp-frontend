# Local Dev Backup (Frontend)

## Setup

1. Copy `env.sample` to your local env file.
2. Install dependencies: `npm install`

## Run

- Start dev server: `npm start`
- Build for hosting: `npm run build`

Default local frontend port is `3000`.
